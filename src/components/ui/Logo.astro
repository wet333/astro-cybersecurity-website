---
interface Props {
    size?: 'sm' | 'md' | 'lg';
    showText?: boolean;
    class?: string;
}

const { size = 'md', showText = true, class: className = '' } = Astro.props;

const sizeMap: Record<string, { box: string; text: string }> = {
    sm: { box: 'h-7', text: 'text-base' },
    md: { box: 'h-9', text: 'text-xl' },
    lg: { box: 'h-12', text: 'text-3xl' },
};
const s = sizeMap[size];
---

<a href="/" class:list={['inline-flex items-center gap-2.5 group', className]} aria-label="Cybershield home">
    <span class:list={['relative inline-flex items-center justify-center', s.box]}>
        <span class="absolute inset-0 rounded-md bg-alert/30 blur-md opacity-60 group-hover:opacity-90 transition-opacity"></span>
        <span class="relative inline-flex items-center justify-center h-full aspect-square rounded-md bg-gradient-to-br from-calm to-alert">
            <i class="fa-solid fa-shield-halved text-ink text-sm" aria-hidden="true"></i>
        </span>
    </span>
    {showText && (
        <span class:list={['font-display font-extrabold text-ink-text tracking-tight', s.text]}>Cybershield</span>
    )}
</a>
